{
  "name": "leomaglove.be",
  "version": "1.0.0",
  "description": "leomaglove.be",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "export": "next export",
    "start": "next start",
    "build": "next build",
    "analyze:server": "BUNDLE_ANALYZE=server npm run build",
    "analyze:web": "BUNDLE_ANALYZE=web npm run build",
    "analyze:all": "BUNDLE_ANALYZE=all npm run build",
    "predeploy": "rimraf node_modules/.cache && npm run build && npm run export && touch out/.nojekyll && touch out/CNAME && echo \"leomaglove.be\" >> out/CNAME",
    "deploy": "git add out/ && git commit -m \"Deploy to gh-pages\" && git push origin `git subtree split --prefix out master`:gh-pages --force"
  },
  "keywords": [],
  "author": "Mathieu Schmidt",
  "license": "ISC",
  "dependencies": {
    "next": "^8.0.1",
    "polished": "^3.0.0",
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-plyr": "^2.1.1",
    "styled-components": "^4.1.3"
  },
  "devDependencies": {
    "@zeit/next-bundle-analyzer": "^0.1.2",
    "babel-plugin-module-resolver": "^3.2.0",
    "babel-plugin-styled-components": "^1.10.0",
    "next-fonts": "^0.17.0",
    "next-images": "^1.0.4",
    "rimraf": "^2.6.3"
  }
}
